let t=3;
let arr1=[ 140, 110, 400];
let arr2= [103, 312, 10];
let arr3= [10, 3, 45];
for(let i=1; i<=t; i++)
{
    let a= arri[0];
    let b= arri[1];
    let c= arri[2];
    let l;
    if((a>b&& b>c) || (c>b&&b>a))
    {
        l=b;
    }
    else if((c>a&&a>b) || (b>a&&a>c))
    {
        l=a;
    }
    else{
        l=c;
    }
    console.lof(l);
}