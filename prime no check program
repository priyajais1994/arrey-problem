// program to check whether a no is prime or not
let n=15;
let count =0; // count for storing the no of factor
for(let j=1; j<=n; j++)
{
    if(n%j==0)
    {
        count = count+1;
    }
}
if(count==2)
{
    console.log("prime no");
}
else
{
    console.log("not prime no");
}